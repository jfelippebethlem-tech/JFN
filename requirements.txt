playwright==1.44.0
python-dotenv>=1.0.0
pandas>=2.2.0
rich>=13.7.0
httpx>=0.27.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
sqlalchemy>=2.0.0
beautifulsoup4>=4.12.0
networkx>=3.3
lxml>=5.2.0
fpdf2>=2.7.0
matplotlib>=3.9.0
ollama>=0.3.0
openpyxl>=3.1.0
# --- Análise/ML (motor de anomalias Onda 1, grafo Onda 3/4, Massare) ---
scikit-learn>=1.5.0
pyod>=2.0.0          # ECOD + IForest (detector de anomalias) — NÃO precisa de torch
duckdb>=1.1.0        # acelera queries analíticas sobre o SQLite (grafo/calibração/eval)
xgboost>=2.0.0       # Massare (massare/ml.py)
hmmlearn>=0.3.0      # Massare (regimes de mercado)
# --- OCR do SEI (CAPTCHA) ---
pytesseract>=0.3.10  # caminho principal (sei_cdp/captcha_solver) — requer Tesseract no SO
opencv-python-headless>=4.9.0
easyocr>=1.7.0       # caminho alternativo do SEI (sei_driver/sei_sei_direct). Puxa torch+torchvision.
# torch/torchvision: instalar SEMPRE a versão CPU para não baixar CUDA (vários GB):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# --- JFN 2.0 (todas GRATUITAS/OSS; instaladas 2026-06-08, núcleo numpy/pandas/scipy/torch intacto) ---
rapidfuzz>=3.14       # dedup de adverse media (Radar, Onda 6)
python-louvain>=0.16  # community detection (import `community`) — Grafo de Poder (Onda 4/6)
igraph>=1.0           # grafo + Leiden (Onda 4/6)
leidenalg>=0.12       # community detection Leiden (Onda 4/6)
pyvis>=0.3            # visualização de grafo HTML (Onda 4)
python-bcb>=0.3       # BCB/Olinda Focus (import `bcb`) — Massare macro (Onda 8)
splink>=4.0           # entity resolution sobre DuckDB (Onda 4)
# DIFERIDAS (instalar na onda que usar):
#   followthemoney  -> precisa libicu-dev + pkg-config (sudo apt); networkx cobre a Onda 4 sem ela
#   sentence-transformers -> embeddings SEI (Onda 5); puxa torch — usar índice CPU (acima)
#   vectorbt        -> backtest (Onda 9); pins estritos de numpy/numba — validar conflito antes
#   weasyprint      -> HTML→PDF (Onda 7); precisa cairo/pango (apt). Alt sem apt: Playwright page.pdf()
python-docx
pdfplumber  # Onda 5: extração de tabela de preços do SEI (camada 1)

# --- O QUE O CÓDIGO IMPORTA E NINGUÉM TINHA DECLARADO (achado pela VM-2 em 2026-07-28,
# montando uma venv NOVA: a coleta do pytest morreu com 8 erros de import antes de rodar um
# único teste). Na VM-1 não aparecia porque a venv é antiga e já os tinha. O custo real não é
# o pytest: `fitz` é a leitura de PDF do SEI (`sei/pdf_texto.py`, `sei/ocr_docs.py`) — uma
# instalação limpa quebrava exatamente na espinha do acervo. Trava: tests/test_requirements_
# declara_o_que_importa.py ---
pymupdf>=1.24.0      # `import fitz` — PDF do SEI (8 módulos); o que matou a venv nova
requests>=2.31.0     # coletores PCRJ/TSE/captcha (não estava em NENHUM requirements)
xlrd>=2.0.1          # `sei/office_texto.py` — planilha .xls antiga anexada ao processo
pypdf>=4.0.0         # manipulação de PDF nos relatórios
pdfminer.six>=20231228  # extração de texto (camada 2 do PDF)
pillow>=10.0.0       # `import PIL` — imagem/foto de medição
cryptography>=42.0.0 # assinatura/credencial
jinja2>=3.1.0        # template de relatório (uso direto, não só via fastapi)
starlette>=0.37.0    # uso direto em `obs_trace.py` (não só como dependência do fastapi)
urllib3>=2.0.0       # uso direto (retry/pool) nos coletores
# oci: NÃO entra aqui de propósito — SDK pesado usado só por `tools/criar_vm_oracle2.py`
# (provisionamento de VM, fora do caminho de análise). Instale sob demanda: pip install oci
