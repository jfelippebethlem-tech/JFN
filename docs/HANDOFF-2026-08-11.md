# Handoff — 10 e 11 de agosto de 2026

> Sessão longa, quase toda de **automelhoria**: medir o que a casa afirma, achar onde a afirmação
> não se sustenta, consertar, e provar o conserto no dado real. O fio condutor do dia foi um só —
> **número sem data, sem base e sem causa não é número** — e ele apareceu em nove lugares diferentes.

---

## 1. O maior: R$ 3,38 bi que a casa somava como ZERO

**Defeito.** A coleta de junho/2026 do SIAFE 1 gravou os campos por **posição**. A tela do SIAFE 1
tem 19 colunas em ordem diferente das 23 do SIAFE 2, então o valor foi parar em `nome_credor`, o
nome do credor em `nl`, e `valor` ficou 0,00. **12.073 linhas da UG 010100, exercícios 2016-2023,
100% de cada par.**

**Por que ninguém viu.** `numero_ob` é a 1ª coluna e foi gravado **certo** — a amostra de números
casava com o espelho e o par saía como **coberto**. Cobertura mede presença; presença não mede
utilidade.

**Reparo, verificado par a par:**

| exercício (UG 010100) | antes | depois |
|---|---:|---:|
| 2016 | R$ 0,00 | R$ 666.741.527,24 |
| 2017 | R$ 0,00 | R$ 615.461.085,17 |
| 2018 | R$ 0,00 | R$ 684.224.971,79 |
| 2019 | R$ 0,00 | R$ 672.912.121,96 |
| 2020 | R$ 0,00 | R$ 739.925.353,31 |
| **total** | **R$ 0,00** | **R$ 3.379.265.059,47** |

Linhas deslocadas: **12.073 → 45**. As 45 que restam são **irreparáveis por recoleta** — a
substituição só alcança o que a tela devolve hoje (medido: no prefixo `2017OB02` o banco tem 639
linhas e a tela devolveu 628). **Apagá-las é decisão do dono**; está declarado em
`limites_de_fonte`, com o filtro que quem listar credor deve usar.

**Fica de infraestrutura:** estado `deslocado` no `cobertura_siafe` (checado ANTES do corte de
cobertura, mais varredura própria a partir do SIAFE) · peso fixo no topo da fila do dreno · card no
painel · invariante `colunas_deslocadas` na `sentinela_integridade` (varre 97 colunas de nome do
banco; só o SIAFE aparecia).

---

## 2. A fila do fiscal parou de acusar o que não podia

Quatro detectores cruzavam tabela de **estado** sem olhar **data**:

| detector | defeito medido | efeito |
|---|---|---|
| `emendas_d3` | 87,9% dos casamentos favorecido×sanção eram de sanção **posterior** à emenda; 10 dos vigentes eram multa/publicação, que **não vedam contratar** | 732 → **48** alertas de severidade alta |
| `pcrj_d9` | 40,3% dos pares tinham o sócio entrando **depois** do exercício da despesa | 171 dos 590 rebaixados |
| `pcrj_d12` | CEP é o cadastro de **hoje**; a base não tem histórico de endereço | data da foto declarada no achado |
| `emendas_d5` | enxerga **6,2%** dos autores e reportava `ok: 0 achado(s)` | cegueira declarada no relatório |

E o `d7_fracionamento` saturava na **contagem** de contratos: **451 de 451** alertas em "alta", do
rente ao teto ao 21×. Agora o **excesso sobre o limite legal** manda, e a distribuição é monotônica
(risco 9 com excesso mediano 3,7×; risco 5 com 1,2×). Fila de severidade alta: **523 → 311**.

**Régua única nova:** `compliance_agent/sancao_impeditiva` — multa e publicação extraordinária são
penalidades que **não** vedam contratar. A regra já existia certa em duas cópias idênticas de SQL;
agora as duas importam de um lugar só, e um teste compara os fragmentos.

---

## 3. Art. 125: o operador de comparação era a questão jurídica

`aditivos_estouro` cortava com `pct >= teto`. O art. 125 admite acréscimos **até** 25% — no teto
exato o contrato está **dentro** da lei. **Quatro dos sete** estouros com acréscimo granular
confirmado estavam em **25,000000%**. Corte agora `> teto + 1e-9` (a tolerância é obrigatória:
`acréscimo/vi` devolve 0,25000000000000006 no teto). Acervo: **247 → 202**; confirmados **7 → 3**.

Dos três, um caiu na conferência: o **VR Benefícios** declara "acréscimo de **25 %**" no próprio
termo, e R$ 20.000 é exatamente 25% de R$ 80.000 — a unidade usou o **valor atualizado**, que é o
que a norma manda. Todo achado passou a sair com `base_do_percentual` declarada.

**Restam dois, ambos com o objeto do próprio termo declarando acréscimo quantitativo:** NEO
Consultoria × TCE-RJ (**125,0%**) e Alexia Decoração × Defensoria Pública (**380,3%**).
Nota: `~/vault/casos/aditivos-acima-do-teto-art125-tcerj-e-dpe.md`.

**Limite de fonte que fecha a questão:** 82,8% dos 1.770 termos do PNCP trazem `valorAcrescido`
zero ou nulo e 59% repetem no objeto do termo o objeto do **contrato**. Na Secretaria das Cidades
(o caso da CGE) são 28 termos com valor zero e natureza indeterminada em 28/28 — **pelo PNCP não há
o que afirmar** sobre aqueles aditivos.

---

## 4. Um detector de fraude religado (X8) — e o falso positivo que eu criei

O cabeçalho da varredura de execução dizia que o **X8 (aditivo retroativo)** ficava fora porque
`contrato_aditivo` não tinha a data de assinatura. Era verdade quando foi escrito e **caducou em
09/08**, quando o coletor passou a gravar `dataAssinatura` (1.684 de 1.770 termos, 95,1%). Ninguém
releu o comentário.

Ligado: **36 contratos** com termo assinado depois do fim da vigência. Conferido à mão — MPRJ ×
AGABO, R$ 3.620.859,02, vigência até 25/11/2024, **1º termo de acréscimo quantitativo assinado em
29/11/2024**.

**Mas a primeira medição deu 48, e 12 eram falsos positivos meus:** o contexto guardava em `data` a
**nova vigência** do termo de prazo, e o X8 usa `data_assinatura or data` — então toda prorrogação
parecia retroativa. Só apareceu porque fui abrir um caso (contrato de R$ 199 mi cujos três termos
têm `data_assinatura` **nula** e mesmo assim vinham confirmados). Corrigido; os 12 viraram
`nao_avaliavel`, que é a resposta honesta.

**Testados e reprovados no mesmo eixo** (para ninguém refazer): X3 — a ponte contrato↔OB por número
de processo casa só **13 de 1.030**; X5 — a única tabela de item é `proposta_item`, de proposta, não
de execução.

---

## 5. O framework de detectores estava MUDO

`data/achados.db` guarda o que as três varreduras produzem — **10.630 avaliações e 543 confirmados
por 13 detectores** — e **nenhuma rota, relatório ou card tocava esse banco**. Não aparecia em
auditoria porque as varreduras rodam **verdes**: fazem o trabalho, só não têm consumidor.

Agora tem `/api/fiscal/detectores` e card, com **confirmado separado de não avaliável** (4.724) —
porque o segundo é a medida da cobertura. E com **frescor por escopo**, porque desde 11/08 16:30 as
varreduras de órgão e certame estão **delegadas à VM-2** e este banco congela para elas.

---

## 6. O CI parou de mandar e-mail — pela causa, não pelo sintoma

O dono recebeu falha em **sete commits seguidos**, todos por regressão minha. A causa não era o
workflow: **nada rodava teste antes do push**. O `pre-commit` roda ruff e os gates do painel, mas
nenhum teste.

`tools/pre_push_gate.sh` + `.git/hooks/pre-push` rodam **as catracas** (1,4 s: `except Exception`
novo, R$ no formato americano, inventário de rotas fora do golden) **e os testes casados** com o que
o push muda — por nome e por **quem importa o módulo**, que é o critério que vale: hoje foram testes
**antigos da casa** que pegaram meu `TypeError` no d3 e minha curva de severidade no d7.

Acima de load 6 ele **declara que não mediu** em vez de aprovar em silêncio. Já me bloqueou duas
vezes — uma delas por um `except OSError: pass` que escrevi **dentro da função criada para acabar
com falha silenciosa**.

---

## 7. Cobertura do SIAFE: 21,1% → 46,5%

Três consertos encadeados, cada um escondendo o seguinte:

1. **Cota anti-inanição** — 257 pares nunca coletados estavam atrás de 551 parciais e nunca
   chegavam à cabeça. A cada 4ª passada eles encabeçam.
2. **A guarda de frescor excluía `nunca_coletado`** — par com zero linhas reprovava no teste "ainda
   incompleto?" e era pulado com "coleta já completa (0 linhas)". Isso **anulava a cota inteira**.
3. **A guarda de carga barrava 10 de 10 passadas** (`load >= 4`, com linha de base 3-5). Trocada
   pela `vm_guard.preflight` da casa, que checa carga **e memória**. De 0-2 para **5 pares por
   passada**.

---

## 8. O que fica ABERTO, e o próximo passo de cada um

### #10 — a busca do SEI está quebrada (DIAGNOSTICADA)
Com a sessão nossa dentro do ciclo, o controle positivo "MGS CLEAN" devolveu **`n_total: 0`** com
**`n_registros: null`** — o formulário submete, o botão é encontrado e clicado, e a **página de
resultado não entrega contagem**. Não é sessão, não é carga, não é ausência do termo.
**Próximo passo:** abrir o SEI com sessão livre e comparar o HTML do resultado com o seletor que
`sei_busca_mgs.py` espera. Provável mudança de layout. Só depois disso a busca por "AGILE CORP" tem
valor — o pedido segue preservado em `data/sei_busca_pedidos.txt`.

### #14 — 2.577 processos lidos que voltaram VAZIOS, R$ 3,78 bi atrás deles
O sweep chamava todos de "fora de escopo/vazio", causa que ninguém mediu. Medido: 378 RESTRITO ·
352 NÃO_LOCALIZADO · 200 RESTRITO? (hipótese) · 216 já capturados por outro caminho · **4 em
contradição** (o registro diz OK e mesmo assim vieram vazios) · **2.577 sem causa nenhuma**.
**Próximo passo:** os 4 em contradição primeiro (`python -m tools.sei_zeros_por_causa
--so-contradicao`), que são diagnóstico barato; depois a fila por exposição (`--fila 40`).

### #8 — cobertura do SIAFE
528 parciais + 256 nunca coletados. O cron avança sozinho a cada 2h, agora com a guarda certa.

---

## 9. As seis famílias novas do catálogo (40-46)

| # | lição |
|---|---|
| 40 | prioridade estrita com vazão baixa **é exclusão disfarçada** — e a vazão está no LOG, não no parâmetro |
| 41 | o **operador de comparação** era a questão jurídica (`>=` vs `>` no teto do art. 125) |
| 42 | a conferência que **confirma** a hipótese não recebe escrutínio — controle positivo vale para a MINHA checagem |
| 43 | a fila do fiscal **não tinha tempo** (3.082 alertas com `created_at` nulo) |
| 44 | orçamento interno não salva do timeout externo: a folga tem de caber **uma unidade inteira (p90)** |
| 45 | a **chave estava certa** e o resto deslocado — cobertura 100% sobre dado inútil |
| 46 | **teste binário num fenômeno gradiente** — três ocorrências no mesmo dia |

---

## 10. Erros meus, para não repetir

- **Publiquei um painel quebrado**: script de patch com dois `assert` e gravação só no fim — o
  segundo falhou, a função nunca entrou, e um script seguinte adicionou a **chamada**. `npm run
  build` e `node --check` passam (nome indefinido é erro de execução) e o `boot_check` foi pulado
  três vezes por carga. **E relatei o oposto do que aconteceu**, deduzindo do traceback em vez de
  conferir com um `grep`.
- **`pgrep -f` matou meu próprio wrapper** três vezes, uma delas levando junto um `git commit`
  encadeado. A receita da casa dizia "confira e mate por PID" — faltava dizer que precisam ser
  **comandos separados**, porque o padrão inline aparece na linha que executa.
- **`git commit` em background**: o git tem **um índice**; um commit pendente leva o que qualquer
  `git add` posterior preparou. Três commits saíram com mensagem que não descreve o conteúdo.
  Medido depois: o mesmo commit **síncrono** leva 40 s. O hábito não economizava nada.
- **Quase publiquei "30 detectores órfãos"** — são **zero**; eu tinha comparado o registro contra
  duas constantes em vez de ler os três runners.


---

# Continuação — 11/08, noite

> Um fio só, de novo, e é irmão do de cima: **o número estava errado porque a UNIDADE de contagem
> estava errada** — linha em vez de processo, texto em vez de dispositivo, folha somada como
> fornecedor. E, em três lugares, a casa já tinha a régua certa numa cópia só.

## 11. A causa do zero já estava no arquivo que a casa lia

`sei_zeros_por_causa` chamava **3.206** processos de "sem causa registrada" consultando só o
`sei_restritos.json`. O progresso do sweep, que ele já abre, guarda `rel` — e `rel > 15` com zero
documento é, **pela regra do próprio `sei_sweep`**, a CAIXA de entrada do SEI: a leitura FALHOU.
São **1.050**, um terço do balde. Precedência: registro apurado (`RESTRITO`) manda mais que sintoma
(`rel`). E `tentativas >= 3` virou ATRIBUTO do item — não diz vazio nem falha, diz que o sweep
desistiu.

**A hipótese que NÃO passou no teste.** Nenhum dos 1.049 foi lido depois de 2026-07-10, a data em
que o código diz que o defeito da caixa foi corrigido; daí escrevi que a releitura renderia. Dois
deles, relidos ao vivo, falharam em ABRIR A ÁRVORE (3 tentativas cada) enquanto o sweep lia outro
processo com 800 documentos no mesmo instante (controle positivo satisfeito). São dois casos e é
pouco para generalizar — mas é bastante para não afirmar o contrário. *"A causa está gravada" não é
"a causa está resolvida".*

## 12. Valor bruto ranqueia FOLHA DE PAGAMENTO, não risco

Quem encabeça o SIAFE por valor é `CG0004700` (**FOLHA DE PAGAMENTOS**, R$ 49,34 bi), `123400` e
`123499` (RIOPREV), `CG0006026` (RIOPREV/INATIVOS) — credor genérico, sem CNPJ. Consequências
medidas, e as duas foram corrigidas:

- **fila do sweep**: no estrato onde ele passa a vida, **30 das 50 primeiras vagas** eram folha; no
  estrato do sinal OSINT, 22. Rebaixadas dentro do estrato (nunca excluídas): 6.527 processos.
- **manchete do painel**: "R$ 9,90 bi atrás dos processos ilegíveis" eram 62% folha e **R$ 601 mi
  de repasse a ente público com CNPJ** (fundos municipais de saúde, Ministério da Fazenda).
  Exposição fiscalizável real: **R$ 3,14 bi**.

Régua única em `compliance_agent/credor_generico` (fornecedor / público / genérico), com filtro de
`status` — quem publica número passa `Contabilizado`.

## 13. Emergência: a régua era cega pela metade E inflada 2,3×

Duas falhas opostas no mesmo detector, achadas ao investigar o caso AGILE.

1. **Cega**: `eh_emergencial` lia o texto do OBJETO. Objeto e enquadramento coincidem em 613 de
   1.504 dispensas do art. 75, VIII — **891 (R$ 2,60 bi) tinham objeto "PEITO DE FRANGO"**. Somando
   o art. 24, IV da 8.666, são 1.001 e R$ 2,80 bi. Foi assim que o DER-RJ (objetos "OBRAS DE
   CONTENÇÃO") nunca apareceu.
2. **Inflada**: `compras_diretas_tcerj` tem uma linha por ITEM e repete o TOTAL do processo em cada
   uma (1.485 de 1.486 processos multi-linha). Somar linha a linha infla o acervo em **2,30×**
   (R$ 39,65 bi × R$ 17,20 bi). O sweep IRMÃO já fazia certo.

Medido depois: **27 grupos, R$ 3,47 bi**. E o screen estava **MUDO** — nenhuma rota, card ou cron o
consumia. Agora tem `/api/fiscal/emergencia_recorrente` e card (11 painéis na fila).

## 14. Caso AGILE/SEEDUC destravou — o processo de R$ 88 mi já estava em casa

`SEI-030001/075841/2024` estava capturado desde 09/08 com **407 documentos**, sob a chave sem o
prefixo `SEI-`, enquanto o progresso o dava como *nunca lido*. Lido:

- **seis lotes** de limpeza escolar do Estado, todos AGILE, todos por dispensa emergencial;
- o **Termo de Referência** e a **cláusula 2.2 do Contrato 31/2024** transcrevem a vedação à
  recontratação do art. 75, VIII (o TR grifa);
- a **COOAIE** escreve que o serviço já estava *"contratado a menos de um ano em outros lotes"*;
- a **licitação do mesmo objeto tramita desde 2022** (`SEI-030029/006362/2022`) sem conclusão;
- o DOD pedia 6 meses, o contrato saiu com 12 — resolvido **trocando o DOD**;
- perguntado se o quantitativo era o mínimo necessário à emergência, a unidade respondeu que tem
  **o mesmo escopo do contrato regular anterior**.

O espelho do TCE-RJ fechou a lacuna que o processo ilegível deixava: junho e novembro de 2024 têm o
**mesmo enquadramento**. Cadeia: SEEDUC 2023 (8.666, art. 24, IV) → SEEDUC 2024 (14.133, art. 75,
VIII) → **FUNESPOM 2025** — 13 processos, R$ 211,6 mi. Total pago AGILE×SEEDUC remedido:
**R$ 685,9 mi** (era R$ 474,8 mi); só 2021 segue truncado.

## 15. Infraestrutura consertada de passagem

| defeito | efeito medido |
|---|---|
| `exit 0` da guarda de carga encerrava o SCRIPT | o gate dos cards-por-clique estava desligado na prática (load > 3 é quase sempre) |
| gate não distinguia card AUSENTE de card EM VOO | bloqueou um commit por cache frio; agora declara NÃO MEDI — e SSE não conta como pendente |
| `sei_consultar --grep` morria em documento sem teor | **385 dos 407** documentos nunca varridos, com cara de busca completa |
| sweep não olhava o acervo | **118** processos íntegros no disco continuavam elegíveis para releitura |
| `.g3` usado em 3 cards e ausente do CSS | grid caía para uma coluna |

## 16. O que fica ABERTO

1. **`SEI-030001/008372/2024`** (diligência 1 do caso AGILE) e **`SEI-030001/040129/2026`**
   (R$ 26,35 mi pagos em 2026, sob instrumento desconhecido): ilegíveis, "SEM ÁRVORE" em todas as
   tentativas. Inscritos em `sei_fila_captura`, que HOJE é consumida pelo sweep.
2. **`SEI-030029/006362/2022`** — a licitação que não conclui desde 2022. Nunca lida.
3. **#10 (busca do SEI) — RESOLVIDO.** Não era layout: o índice de texto livre do SEI é sobre
   **DOCUMENTOS**, e a ferramenta submetia com `Considerar Documentos` desmarcado — escopo em que a
   busca varre só metadado de processo e devolve zero honestamente, para qualquer termo (por isso o
   controle positivo também dava zero, e a culpa parecia nossa). Marcada a caixa, `LIMPEZA` devolve
   **213.563**. Somam-se dois enganos de leitura: a contagem é `Exibindo 1 - 10 de N`, não
   `(N registros)`; e `Nenhum resultado encontrado` é **template escondido**, presente no HTML mesmo
   com 213.563 achados. `parse_resultado` passa a devolver três estados, e `nao_parseei` nunca é
   zero. Falta só rodar o pedido represado ("AGILE CORP"): a busca exige `load < 1,7` e a sessão
   única do IP, e concorre com o sweep.
4. **A fatia "sem causa" do SEI** provavelmente é falta de ACESSO — os dois testes ao vivo apontam
   para lá. O registro de restritos é que deveria recebê-la.


---

## 17. 🔴 ABERTO E URGENTE — `compliance.db` com corrupção REAL (12/08, madrugada)

**Não é** o caso conhecido (`-shm` morto no processo, que o guardião cura com restart): naquele o
`quick_check` volta `ok`. Aqui não volta — e a nota da casa diz o que fazer quando não volta:
*perícia de dado, não reboot*.

| rootpage | objeto | dano |
|---|---|---|
| 7 | **TABELA `ordens_bancarias`** | 155 × *invalid page number* — **24.660 linhas ilegíveis (2,13%)** |
| 21 · 293403 · 759469 | índices de `ob_orcamentaria_siafe` | *2nd reference to page* — `REINDEX` resolve, sem perda |

**`ob_orcamentaria_siafe`, a fonte canônica de OB, está ÍNTEGRA** (568.210 linhas). O danificado é
o espelho TFE, que é recoletável.

**A armadilha que me enganou por horas:** `COUNT(*)` responde normalmente (conta pelo ÍNDICE, cujas
entradas sobrevivem à página de dado quebrada) e `SUM(valor)` é que estoura. Por isso classifiquei
duas falhas de `test_golden_numbers` como "drift de dado" no relatório anterior — **erro meu**. Os
goldens estavam certos: golden MGS = 1.239 OBs e o legível é 1.173 (faltam as 66 auditadas em
03/08); golden ITERJ = 2.572, legível 2.483. Golden que cai para BAIXO não combina com coleta, que
só acrescenta — a pista estava à vista.

**Já feito, tudo reversível e sem tocar no original:**
- backup integral: `/home/ubuntu/compliance.db.backup-20260812-0040` (3,4 GB; 92 G livres no disco);
- perícia: `tools/salvar_tabela_corrompida.py` — varre por faixas de `rowid` e subdivide até isolar
  a linha ilegível; abre em `mode=ro`;
- salvação: `/home/ubuntu/ordens_bancarias_salvo.db`, **1.133.092 linhas** (493 MB).

### RESOLVIDO em 2026-08-12 08:10, com autorização do dono

Reconstrução em **arquivo NOVO**, não conserto no lugar: `DELETE`+`INSERT`+`REINDEX` escreveria num
arquivo com páginas quebradas — pode ampliar o dano, e as páginas ruins seguem na lista livre para
reuso. O original foi aberto SEMPRE em `mode=ro` e está preservado.

| medida | resultado |
|---|---|
| tabelas comparadas | **99 · 97 idênticas** origem×destino |
| `ordens_bancarias` | 1.142.056 recuperadas · **17.395 irrecuperáveis deste arquivo** (1,50%) |
| `ob_orcamentaria_siafe` | **566.571, íntegra** — só os ÍNDICES estavam corrompidos |
| `integrity_check` do novo | **ok** |
| `sqlite_stat1` | regenerado por `ANALYZE` (189 entradas, 4 s) |
| serviço | `/api/ugs` responde 200 (era 500) · boot_check ok · 11 painéis na tela |

**Duas descobertas que mudaram a conta.** (1) `COUNT(*)` mente: a origem devolvia 568.210 e 569.179
para a MESMA tabela, e a varredura real entrega 566.571 — a contagem passa pelo índice e sobrevive
à página de dado quebrada. Por isso `_contar` usa `COUNT(rowid)`. (2) A tabela do SIAFE **não tinha
dano de dado**: copiando sem a PK dá 566.571 linhas, zero duplicata, zero ilegível. O que estava
quebrado eram os três índices, e eles nascem limpos na reconstrução.

**Arquivos preservados** (nada foi apagado):
`data/compliance.db.corrompido-20260812-0810` (+ `-wal`, `-shm`) ·
`/home/ubuntu/compliance.db.backup-20260812-0040` · `/home/ubuntu/ordens_bancarias_salvo.db` ·
laudo em `/home/ubuntu/laudo_reconstrucao.json`.

**FICA ABERTO — passo 4: recoletar as 17.395 OBs.** Enquanto não forem recoletadas,
`test_golden_numbers` FALHA de propósito: MGS 1.175 contra 1.239 congelados, ITERJ 2.485 contra
2.572. **Não recarimbar esses goldens** — a falha é o sinal de que a recoleta não aconteceu.
A fonte canônica (SIAFE) está intacta, então a perda é do espelho TFE e é recoletável.


---

## 18. Recoleta feita — e uma pergunta que fica ABERTA

**Recoleta das OBs perdidas (passo 4), concluída em 2026-08-12.** Fonte: `collectors/tfe_ob`, que
ingere por ano e é idempotente (`DELETE` do exercício + `INSERT`). Baixei o ZIP fresco (o cache era
de 21/06, o que teria perdido dois meses de 2026) e **conferi a contagem de cada ano ANTES de
qualquer DELETE** — a ingestão apaga o exercício antes de inserir e não tem guarda contra fonte
vazia.

| | antes | depois |
|---|---:|---:|
| `ordens_bancarias` | 1.142.056 | **1.178.076** (+36.020) |
| tfe_ob 2024 | 157.823 | 176.124 |
| tfe_ob 2025 | 186.229 | 192.986 |
| tfe_ob 2026 | 107.868 | 118.830 |

Recuperou mais que as 17.395 perdidas porque a fonte publicou muita coisa desde a última ingestão.
Alvo identificado antes de agir: **221 pares (UG, exercício)**, sete deles com ~80% da perda
(296100/2026, 166100/2026, 136200/2026, 266500/2025, 294200/2024, 404340/2025, 166100/2025) —
localizados pelos vizinhos de rowid das linhas ilegíveis.

**Goldens revisados DE PROPÓSITO, com auditoria linha a linha** (`test_golden_numbers` volta a
passar):
- **MGS 1.239 → 1.238** (−R$ 85.653,26): não é a corrupção, é DESPUBLICAÇÃO. A conta fecha nos
  centavos — é a `2022OB00184`, preservada em `ob_retirada` desde 10/08.
- **ITERJ total +R$ 52.440,73**, mesma contagem (2.572) e mesmos 198 fornecedores: comparando a UG
  inteira contra a salvação pré-recoleta, 89 linhas RECUPERADAS e **zero mudança de valor** — o
  delta já existia antes da corrupção. Revisão da fonte. Não digo qual OB mudou: não há snapshot
  anterior ao drift.
- **pct_cnpj_min 76 → 75**: composição, não perda. O acervo tem 83,9% de CNPJ em 2020 e 73,4% em
  2025 / 74,3% em 2026 — exercício recente paga mais a CPF e a credor genérico, e trazer 36 mil
  linhas de 2024-2026 puxou a média para 75,23%.

### ABERTO: o baseline de achados mudou e eu NÃO consegui atribuir a causa

`test_baseline_achados` acusa **C9 · crítica 13 → 4** (e o conjunto C9 caiu de 57 para 41 achados).
C9 é "fornecedor pago majoritariamente FORA de contrato regular (TAC/indenização)" — é um achado
de peso. **Não recarimbei o baseline**, porque a regra da casa é dizer POR QUE cada linha mudou.

Quatro hipóteses testadas e EXCLUÍDAS:
1. **mudança de código** — o C9 e o `detector_tac` não mudam desde 2026-08-04, antes do baseline;
2. **taxa de TAC da unidade** (o C9 rebaixa quem está "na norma" da própria unidade) — medida nas
   seis UGs mais afetadas, praticamente imóvel (296100: 2,8% → 2,8%; 294200: 26,8% → 27,0%);
3. **denominador maior pela recoleta** — medido nos 14 CNPJs que carregam C9: o percentual caiu em
   5 e SUBIU em 9, com variação de décimos;
4. **degradação pela corrupção** (a hipótese mais promissora: `tac_por_cnpj` estourando viraria
   `nao_avaliavel`) — reavaliei os 41 processos afetados com o banco já reparado, 40 gravados, e os
   números ficaram **idênticos** (4 crítica · 21 alta · 16 média).

### FECHADO no mesmo dia — e a hipótese 4 estava CERTA; meu teste é que errou a população

O que faltava era um snapshot anterior. A casa tem backup off-box semanal, e havia um de
**2026-08-09 03:50** — o mesmo dia do baseline. Baixado (831 MB) e conferido: `quick_check = ok` e
**2.239 avaliações**, exatamente o `_processos_avaliados` do baseline.

Com ele, o diff que faltava saiu em um comando: **18 processos PERDERAM o achado C9**, 2 ganharam,
**zero mudou de grau**. E o carimbo de tempo fecha a questão: **17 foram reavaliados entre 03:00 e
03:59 de 12/08 e 1 às 07:xx** — dentro da janela de corrupção, que só terminou às 08:10 com o
reparo.

**Por que eu havia "excluído" essa hipótese:** reavaliei os processos que AINDA TINHAM C9, quando
os afetados eram justamente os que o PERDERAM. Testar a população errada dá um verde falso — e o
verde falso é pior que o vermelho, porque encerra a investigação.

Reavaliados com o banco são, os achados voltaram **exatos**: C9 crítica 13 (= baseline), média 16
(= baseline), alta 29 (baseline 28, +1 do processo que ganhou). Um deles, o `080002/020895/2024`,
tinha ido de **EXTREMO (score 80) para BAIXO (score 0), com zero achados** — a corrupção zerou um
processo de risco extremo, em silêncio.

**Varredura completa da janela:** comparando achado a achado contra o backup, apenas **2 processos**
seguem com menos achados, e nenhum é corrupção — `260007/003321/2026` (a captura completou de 27
para 35 documentos e a lacuna de planejamento sumiu porque os documentos apareceram) e
`270042/000561/2023` (perdeu um C2, da família de fachada, com os MESMOS 40 documentos; reavaliar
com banco são não o traz de volta — é mudança no dado de due diligence, sem efeito agregado).

**Baseline recarimbado**, agora com a auditoria feita: o resto do diff (+7 MEDIO, +6 A3, +1
AC_RESSALVA, +1 F_CONTRATACAO, +2 X7 crítica, +6 X_PAGAMENTO_SEM_ATESTACAO) vem dos **101 processos
novos** avaliados desde 09/08 — nenhum processo desapareceu do acervo, e das 17 mudanças de faixa
nos que já existiam, 15 são `NAO_AVALIAVEL` → avaliável, isto é, captura completando.


---

## 19. ABERTO — a flag de pausa do sweep some sozinha

Criei `data/.pause_sei_sweep` às **01:03** de 12/08 (702 bytes, com o motivo escrito dentro) para
proteger o banco corrompido de gravadores. Às **07:33** ela não existia mais, e o sweep tinha
voltado a rodar.

Procurado e NÃO encontrado: nenhuma linha do repositório a remove. Varri `tools/` e
`compliance_agent/` por `rm`, `unlink`, `missing_ok` e `find -delete` — os cinco pontos que a citam
(`sweep_sei.sh`, `sei_supervisor.sh`, `sweeps_serial.sh`, `sweep_sei_noturno.sh`,
`ler_medviva_fserj.sh`) são todos LEITORES, exceto o último, que só faz `touch`. Também não é
cron (nenhum job de limpeza toca `data/.pause*`), não é git (a flag é `.gitignore`-ada e não
rastreada, então só `git clean -fdx` a alcançaria, e não rodei), e não havia outra sessão de agente
na máquina.

Duas observações que estreitam o cerco para quem retomar:
- `.pause_fachada_streetview_sweep` e `.pause_serie` **sobreviveram** no mesmo diretório, o que
  descarta limpeza por glob amplo;
- há um glob perigoso dormindo em `tools/migracao_bringup.sh:83` — `rm -f data/.pause_sweeps
  data/.pause_*_sweep` — que apagaria TODAS as pausas de uma vez. Ele exige `--ativar`, não está em
  cron nem em unidade systemd, e não rodou; mas é a linha que faria exatamente isto.

**Por que importa:** a pausa é o mecanismo de proteção da casa (foi o que usei durante o reparo do
banco). Um mecanismo de proteção que evapora sem explicação não protege — e a lição já catalogada
sobre pausa que se auto-remove diz que o cron entra no vão e toma a sessão itkava.
