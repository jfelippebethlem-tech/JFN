================================================================================
SIAFE-RIO 2 — EVOLUÇÃO DAS NOSSAS TENTATIVAS (todo o repo)
Documento de inteligência para consulta. Gerado em 2026-06-06.
================================================================================

OBJETIVO
--------
Extrair do SIAFE-Rio 2 (siafe2.fazenda.rj.gov.br) a execução financeira — sobretudo
as Ordens Bancárias (OB = pagamento DEFINITIVO) com a coluna PROCESSO (nº SEI), os
CONTRATOS por fornecedor, e correlacionar OB <-> contrato <-> processo SEI. O SIAFE é
um Oracle ADF Faces (MyFaces Trinidad), que renderiza por PPR (AJAX parcial) e tem
gatilhos que resistem à automação ingênua. Abaixo, tudo que tentamos, o que aprendemos
e o que JÁ FUNCIONA.

--------------------------------------------------------------------------------
1. LINHA DO TEMPO (resumo dos commits, do +antigo ao +novo)
--------------------------------------------------------------------------------
31/05  Agente SIAFE2/SEI inicial (Playwright + Claude API). SIAFE_EXERCICIO=2026.
01/06  diagnose_siafe.py: explora menus, FlexVision, hashes e formulários via CDP.
       siafe_browser.py reescrito com seletores reais; navegação Vaadin/ADF.
       Deep-dive de Execução com captura de SEI; "lock in" dos seletores ADF de OB.
02/06  Extrai favorecido/CPF/valor/processo DIRETO da tabela; coleta 2026 destravada.
       Suite de testes offline + observabilidade da coleta.
04/06  >>> BREAKTHROUGH DO FILTRO ADF (contratos) <<<
       - Descoberto que o texto "Filtro" (pnlAccordionDec_afrCl0) é ARMADILHA
         (onclick="return false"); quem abre o painel é o disclosure sdtFilter::disAcr.
       - Decifrado: Propriedade/Operador via select_option; o VALOR exige keyboard.type
         (keystrokes reais) + Enter — fill() NÃO dispara a query do ADF.
       - siafe_contratos.py FUNCIONAL: lista TODOS os contratos por CNPJ.
         VALIDADO: MGS CLEAN = 41 contratos, R$ 146,7 mi.
       - Pacote compliance_agent/auditoria/ (auditar_fornecedor: TFE + contrato + SEI).
       - Protocolo de operação SEGURA (ritmo humano anti-bloqueio).
05/06  Coletor SIAFE autônomo (sem CDP manual). Login robusto + diagnóstico.
       MFA: SIAFE passou a exigir 2FA -> suporte a MFA via Telegram / SIAFE_MFA_CODE.
       Vários fixes de login ADF: cbxCliente/cbxExercicio, popup "usuário já logado"
       (clicar "Sim" com mouse REAL — ADF ignora JS click; quirk 0x0 do CSS),
       logout entre exercícios, "context destroyed", lista de anos sem futuros.
       Coleta de OBs via GitHub Actions (IP Azure) — workflows criados.
06/06  siafe_ppr.py: replay HTTP do PPR (scroll) p/ furar o limite de 50 da view.
       siafe_ob_orcamentaria.py: varredura por SCROLL da tabela virtualizada.
       docs SIAFE-ARQUITETURA / PESQUISA-SIAFE-ADF-PPR / NAVEGACAO / GUIA-AUTOMACAO.
       (sessão atual) Achados ao vivo da VM — ver seção 4.

--------------------------------------------------------------------------------
2. FERRAMENTAS QUE EXISTEM NO REPO (e status)
--------------------------------------------------------------------------------
compliance_agent/siafe_login.py         Login direto da VM (Playwright headless).
compliance_agent/siafe_session.py       Login c/ MFA + persistência de sessão 24/7.
compliance_agent/siafe_coord.py         Coordenação da SESSÃO ÚNICA via Telegram
                                        (SIAFE só aceita 1 sessão por usuário).
compliance_agent/siafe_ob_orcamentaria.py  Varredura da tela "OB Orçamentária"
                                        (tabela pt1:tblOBOrcamentaria). Colhe rolando
                                        o ::scroller; ingere em ob_orcamentaria_siafe.
                                        Tem coletar_resiliente (retoma se a sessão cair)
                                        e chkRemoveLimit (ver seção 4 — ausente nesta tela).
compliance_agent/siafe_ppr.py           Coleta via replay HTTP do PPR (scroll), tabela
                                        pt1:tblOrdemBancaria. Fura o limite de 50/view.
compliance_agent/collectors/siafe_ob.py Coletor diário OB Orçamentária.
compliance_agent/auditoria/siafe_contratos.py  >>> O QUE FUNCIONA <<< lista TODOS os
                                        contratos por CNPJ via filtro ADF (keyboard.type).
compliance_agent/auditoria/auditar_fornecedor.py  Orquestrador 1-comando (TFE+contrato+SEI).
siafe_agent/ (agent.py, browser/siafe_browser.py, llm/groq_explorer.py)  Agente autônomo
                                        de exploração (loop agêntico sem Claude API).
diagnose_siafe.py                       Diagnóstico: coleta seletores reais.
.github/workflows/*siafe*               Coleta headless por GitHub Actions (IP Azure).
DOCS: docs/SIAFE-ARQUITETURA.md, docs/PESQUISA-SIAFE-ADF-PPR.md, docs/SIAFE-NAVEGACAO.md,
      docs/SIAFE-RIO2-GUIA-AUTOMACAO.md, _SANDBOX/SIAFE-rotina-auditoria.md,
      _SANDBOX/gcp/briefing-VM-siafe-auditoria.md, SIAFE_AGENT_LOG.md (diário de erros/fixes).

--------------------------------------------------------------------------------
3. APRENDIZADOS TÉCNICOS (o miolo)
--------------------------------------------------------------------------------
LOGIN (login.jsp):
  - Campos: loginBox:itxUsuario::content, loginBox:itxSenhaAtual::content,
    cbxCliente::content (0=Rio), cbxExercicio::content (2027=0,2026=1,2025=2,2024=3,2023=4),
    botão btnConfirmar.
  - Popup "usuário já logado / Deseja continuar?": clicar "Sim" (myBtnOk) com MOUSE REAL
    (Playwright native click) — ADF ignora el.click() de JS; o botão pode estar 0x0 no CSS.
  - SESSÃO ÚNICA: logar derruba a sessão do Jorge e vice-versa -> coordenação via Telegram.
  - MFA/2FA: passou a ser exigido às vezes; injeção via SIAFE_MFA_CODE / código no Telegram.
  - Exercícios liberados na conta: 2024-2026 (2023 bloqueado pelo servidor — detectar e pular).

NAVEGAÇÃO (sem derrubar a sessão):
  - Menus ADF (disclosureAnchor) por índice; PREFIXO varia por tela (pt1/tpl/ptPrincipal).
  - Clicar por COORDENADA/elemento real, aguardar 2-3s, confirmar pela URL/título.
  - NÃO usar el.click() de JS em link de menu com o popup myModal ativo (encerra a sessão).

ADF / PPR (Partial Page Rendering):
  - A página atualiza pedaços via AJAX. Campos aparecem só após eventos (ex.: o campo
    "Valor" do filtro só nasce DEPOIS de escolher o Operador e dar blur).
  - Eventos SINTÉTICOS (dispatchEvent change/blur) e fill() NÃO disparam o autoSubmit/PPR
    de forma confiável em headless. É a origem de quase toda a dor.

>>> O SEGREDO DO FILTRO ADF (decifrado 04/06, VALIDADO em contratos) <<<
  1. O texto "Filtro" (pnlAccordionDec_afrCl0) é ARMADILHA (onclick="return false").
     Quem ABRE o painel é o disclosure  PREFIXO:TABELA:sdtFilter::disAcr  (title "Mostrar
     este painel"). Ex.: pt1:tblContrato:sdtFilter::disAcr (contratos),
     pt1:tblOrdemBancaria:sdtFilter::disAcr (OB), pt1:tblOBOrcamentaria:sdtFilter::disAcr.
  2. Propriedade (cbx_col_sel_rtfFilter::content) e Operador (cbx_op_sel_rtfFilter::content)
     são <select> nativos -> select_option() FUNCIONA neles.
       Operadores: 0=igual,1=>,2=>=,3=<,4=<=,5=!=,6=nulo,7=contém,8=começa,9=termina,10=pertence(;)
  3. O VALOR (in_value_rtfFilter::content) PRECISA ser DIGITADO com keyboard.type(valor, delay=80)
     + keyboard.press("Enter"). fill()/dispatchEvent NÃO aplicam o filtro. ESTE é o pulo do gato.
  Receita validada (contratos, MGS): abrir sdtFilter::disAcr -> Propriedade=7 (Cod. Contratado)
  -> Operador=0 (igual) -> clicar o campo Valor, Ctrl+A/Delete -> keyboard.type(cnpj) -> Enter.
  RESULTADO: 41 contratos, R$ 146,7 mi.

LIMITE DE 1000 REGISTROS:
  - As consultas trazem no máximo ~1000 linhas por vez (corte do servidor na carga).
  - Algumas telas têm checkbox chkRemoveLimit que remove o teto (ex.: Acompanhamento de PD,
    Código de Barras, Guia de Devolução). A tela OB Orçamentária NÃO tem esse checkbox.
  - Estratégia p/ >1000: segmentar com o filtro (UG Emitente, Data Emissão por faixa,
    Número "começa com", "pertence ;") e iterar. Órgãos grandes (Casa Civil, SEEDUC, SES,
    ALERJ, TJRJ) têm >1000 OBs/ano -> precisam de sub-filtro por período além da UG.

PPR via HTTP (alternativa ao browser — siafe_ppr.py / docs/PESQUISA-SIAFE-ADF-PPR.md):
  - Usa Playwright só p/ logar; depois pagina com context.request (mesmos cookies),
    disparando o evento de SCROLL via POST e lendo o envelope ADF (<fragment><![CDATA[...]]>).
  - O javax.faces.ViewState ROTACIONA: extrair o novo a cada resposta.
  - Fura o limite de 50/view; replicar o FILTRO por HTTP (não só scroll) é o caminho
    documentado p/ extração completa sem o browser.

--------------------------------------------------------------------------------
4. ACHADOS AO VIVO DESTA SESSÃO (2026-06-06, da VM)
--------------------------------------------------------------------------------
  - A VM ACESSA O SIAFE: siafe2.fazenda.rj.gov.br respondeu HTTP 200 da VM (GCP); login +
    navegação OK (≈14s login, ≈28s nav). DIFERENTE do SEI, o WAF do SIAFE não dropa a VM.
  - Coletadas/ingeridas 951 OBs de 2026 pela tela OB Orçamentária (sem filtro).
  - CONFIRMADO o teto: ob_orcamentaria_siafe tem 2024=995, 2025=971, 2026=951 — todos no ~1000.
  - A tela OB Orçamentária NÃO tem chkRemoveLimit (degrada com log honesto).
  - O seletor selUg (topo) NÃO filtra a listagem (testado select_option + teclado: distribuição
    de UGs idêntica, sem glasspane). selUg é contexto, não filtro de grade.
  - O sdtFilter da OB Orçamentária EXISTE (sondado): Propriedade tem 24 opções (Número=0,
    UG Emitente=1, UG Pagadora=2, Data Emissão=3, ..., Processo=16); Operador 12 opções.
  - ERRO DESTA SESSÃO (corrigido pelo histórico): tentei o filtro com fill()+dispatchEvent e
    select_option no valor -> 0 PPR. O MÉTODO CERTO (já provado em 04/06) é keyboard.type+Enter
    no campo Valor. >>> Próximo passo: aplicar keyboard.type na OB Orçamentária p/ furar o 1000.

--------------------------------------------------------------------------------
5. O QUE JÁ FUNCIONA HOJE
--------------------------------------------------------------------------------
  [OK] Login na VM (com tratamento de popup/MFA/sessão única).
  [OK] CONTRATOS por fornecedor: filtro ADF via keyboard.type (siafe_contratos.py). MGS=41.
  [OK] OB Orçamentária: varredura por scroll até ~1000/consulta + ingestão (rico: 23 colunas,
       inclui PROCESSO/SEI que o TFE não tem).
  [OK] Base de VOLUME de OBs já existe por OUTRA via: 1.121.303 OBs (2019-2026) do DOWNLOAD TFE
       (dados abertos, sem SIAFE/MFA/ADF) em ordens_bancarias. O scrape SIAFE agrega o nº SEI.
  [OK] Correlação OB<->SEI<->contrato (correlacao_sei.py + tcerj_aberto): nº SEI em 1.527 OBs,
       31 links OB<->contrato por SEI, 466k OBs casadas por CNPJ.

--------------------------------------------------------------------------------
6. O QUE FALTA / OPÇÕES PARA DECIDIR
--------------------------------------------------------------------------------
  (A) OB Orçamentária COMPLETA (>1000/ano): aplicar a técnica keyboard.type no filtro
      (Propriedade=UG Emitente ou Data Emissão por faixa) e iterar por UG/período. É a
      continuação natural do que já funciona em contratos. Custo: runtime (browser, lento)
      + sessão única (coordenar c/ o Jorge). Cada UG <1000 -> uma consulta; UGs grandes ->
      sub-faixa de período.
  (B) PPR por HTTP com FILTRO: estender siafe_ppr.py p/ replicar o request de filtro (não só
      scroll) e paginar. Mais robusto/rápido, sem browser; exige decifrar o payload do filtro.
  (C) Ficar com o TFE (1,12M OBs) como volume e usar o SIAFE só p/ o que ele tem de único
      (PROCESSO/SEI) no alcançável — pragmático, já quase pronto.

  Recomendação: (A) reusando a receita de contratos (keyboard.type) — menor risco, já provada.

--------------------------------------------------------------------------------
7. ARMADILHAS A NÃO REPETIR (lições)
--------------------------------------------------------------------------------
  - fill()/dispatchEvent/select_option NO CAMPO VALOR do filtro = não dispara PPR. Use keyboard.type+Enter.
  - Clicar o label "Filtro" (onclick=return false) = nada. Use sdtFilter::disAcr.
  - el.click() de JS em menu/popup ADF = pode encerrar a sessão. Use clique real (mouse).
  - selUg do topo NÃO filtra a grade da OB Orçamentária.
  - SIAFE = sessão única: combinar antes de logar (siafe_coord / Telegram).
  - 2023 bloqueado p/ a conta; só 2024-2026.

================================================================================
FIM — dúvidas? Os detalhes finos estão em docs/SIAFE-*.md e SIAFE_AGENT_LOG.md.
================================================================================

================================================================================
8. ESTRUTURA DOM COMPLETA DO SITE (mapa de IDs — para automação)
   (conteúdo integral do docs/SIAFE-RIO2-GUIA-AUTOMACAO.md)
================================================================================

# SIAFE-RIO 2 — Guia Técnico de Automação por IA (Execução Financeira)

> **Fonte:** mapeamento manual do Mestre Jorge após login (06/06/2026). Versão SIAFE 4.167.12, build
> 202605281616, exercício 2026. **Inteligência operacional** para o coletor `siafe_ob_orcamentaria.py`
> e correlatos. Complementa `docs/SIAFE-ARQUITETURA.md` e `docs/PESQUISA-SIAFE-ADF-PPR.md`.
>
> ⚠️ A mensagem-fonte foi **truncada** na seção 7.6 (Guia de Recolhimento, lista de botões). As seções
> 1–7.5 estão completas; 7.6+ a completar quando o Mestre enviar o restante.

- **URL base:** `https://siafe2.fazenda.rj.gov.br/Siafe/`
- **IP servidor:** 10.8.180.234 (rede interna — WAF gov barra IP não-governamental, ver CLAUDE.md §6)

---

## 1. Arquitetura técnica

- **Framework:** Oracle ADF (Application Development Framework) + Apache MyFaces Trinidad.
- **Padrão de IDs:** `prefixo:componente:subcomponente::tipo`. O **prefixo varia por tela**:
  - Lista/tabela principal: `pt1:`
  - Acompanhamento PD: `tpl:`
  - Fechamento do Dia: `ptPrincipal:`
  - Tipo de Conciliação: `tpl:`
- **Forms:** lista = `id="frmPrincipal"`; detalhe = `id="frmDocumento"`.
- **Hidden fields (sessão):**
  - `oracle.adf.view.faces.RICH_UPDATE` (hidden, value="dirty")
  - `org.apache.myfaces.trinidad.faces.FORM` (hidden)
  - `Adf-Window-Id` (hidden, ex.: "w8")
  - `javax.faces.ViewState` (hidden, **token de sessão**)
- **Push iframe (ADF polling):** `id="afr::PushIframe"`.
- **PPR (Partial Page Rendering):** o ADF atualiza partes da página via AJAX sem recarregar. **Crítico:**
  o campo "Valor" do filtro só aparece no DOM **após** o servidor fazer PPR em resposta ao blur/change
  do campo "Operador".

---

## 2. Login

- **URL:** `.../Siafe/faces/login.jsp`
- **Campos:**
  - `loginBox:itxUsuario::content` (text, name=`loginBox:itxUsuario`)
  - `loginBox:itxSenhaAtual::content` (password, name=`loginBox:itxSenhaAtual`)
  - `loginBox:cbxCliente::content` (select) — `value="0"` = Rio de Janeiro
  - `loginBox:cbxExercicio::content` (select) — options: Selecione, **2027(0), 2026(1), 2025(2), 2024(3), 2023(4)**
  - `loginBox:btnConfirmar` (submit) — botão "Ok"
- **Popup "USUÁRIO LOGADO" (sessão duplicada):**
  - `myBtnOk` (text="Sim") — confirma e encerra a outra sessão
  - `myBtnCancel` (text="Não") — cancela
- ⚠️ **Gotcha:** usar `el.click()` via JS num link do menu enquanto o popup `myModal` está ativo **encerra
  a sessão**. Solução: clicar visualmente, ou checar se `myModal` está visível antes.
- **URL de erro de sessão:** `.../Siafe/faces/session-error.jsp` ("Sua sessão foi encerrada devido a uma
  conexão em outro dispositivo" + link "Retornar à tela de Login").

---

## 3. Estrutura de navegação

**Menu principal (abas nível 1):** `pt1:pt_np4:N:pt_cni6::disclosureAnchor`
`0=Planejamento · 1=Execução · 2=Projetos · 3=Apoio · 4=Administração · 5=Relatórios`

**Subabas de Execução (nível 2):** `pt1:pt_np3:N:pt_cni4::disclosureAnchor`
`0=Execução Orçamentária · 1=Execução Financeira · 2=Contabilidade · 3=Contratos e Convênios ·
4=Folha de Pagamento · 5=Mensagens · 6=Acompanhamento Execução`

**Menu lateral — Execução Financeira (20 itens):** `PREFIXO:pt_np2:N:pt_cni3` (PREFIXO varia por tela)

| Índice | Label | URL JSP |
|---|---|---|
| 0 | Acompanhamento de Execução de PD | `acompanhamentoExecucaoPDCad.jsp` |
| 1 | Bloqueio Judicial | `bloqueioJudicialCad.jsp` |
| 2 | Código de Barras | `codigoBarrasCad.jsp` (estimado) |
| 3 | Fechamento do Dia | `pagamentoCad.jsp` |
| 4 | Guia de Devolução | `guiaDevolucaoCad.jsp` |
| 5 | Guia de Recolhimento | `guiaRecolhimentoCad.jsp` |
| 6 | Lista de Favorecido para OB | `listaFavorecidoOBCad.jsp` |
| 7 | Nota de Aplicação e Resgate | `notaAplicacaoResgateCad.jsp` |
| 8 | Ordens Bancárias | `ordemBancariaCad.jsp` |
| 9 | OB Orçamentária | `ordemBancariaOrcamentariaCad.jsp` |
| 10 | OB de Dedução | `ordemBancariaDeducaoCad.jsp` |
| 11 | OB de Retenção | `ordemBancariaRetencaoCad.jsp` |
| 12 | OB de Transferência | `ordemBancariaTransferenciaCad.jsp` |
| 13 | OB Extra-orçamentária | `ordemBancariaExtraOrcamentariaCad.jsp` |
| 14 | Programações de Desembolso | `programacaoDesembolsoCad.jsp` |
| 15 | PD Orçamentária | `programacaoDesembolsoOrcamentariaCad.jsp` |
| 16 | PD de Retenção | `programacaoDesembolsoRetencaoCad.jsp` |
| 17 | PD de Transferência | `programacaoDesembolsoTransferenciaCad.jsp` |
| 18 | PD Extra-orçamentária | `programacaoDesembolsoExtraOrcamentariaCad.jsp` |
| 19 | Tipo de Conciliação Bancária | `tipoConciliacaoBancariaCad.jsp` |

**Navegar sem encerrar sessão:** (1) clicar por coordenada visual (não `el.click()` via JS); (2) aguardar
2–3s; (3) confirmar pela URL/título.

---

## 4. Seletor de UG (em todas as telas)

- **ID:** `PREFIXO:selUg::content` (name=`PREFIXO:selUg`), `SELECT` (select-one). Padrão `value="0"` = TODAS.
- ⚠️ O `value_option` é o **índice da option no exercício 2026** — pode mudar entre exercícios (em 2026
  surgem UGs como 016200/026200 que deslocam índices). Casar por **código da UG**, não pelo índice.

Mapa completo extraído em `data/siafe_ug_map_2026.json` (gerado deste guia). Referência (código | nome | value):

```
0=TODAS(0)
010100 ALERJ(1) · 016100 FUNDO ALERJ(2) · 016200 FUNPGALERJ(2026) · 020100 TCE-RJ(3) · 026100 FEM/TCE-RJ(4)
026200 FUNPGT(2026) · 030100 TJ(5) · 036100 FETJ(6) · 036200 FEEMERJ(7) · 036300 FUNARPEN(8)
040100 SEPLANIG-Extinta(9) · 043400 AGETRANSP(10) · 043500 AGENERSA(11) · 044100 DER-RJ(12) · 045200 EMOP(13)
046500 FRSCPER(14) · 050100 SEDC-Extinta(15) · 053100 IPEM(16) · 060100 GSI(17) · 070100 SEINFRA-Extinta(18)
070200 CEDAE ACOES DESC.(19) · 080100 VICE-GOV(20) · 090100 PGE(21) · 096100 FUNPERJ(22) · 100100 MP(23)
100200 CEJ(24) · 106100 FEMP(25) · 110100 DPGE(26) · 116100 FUNDPERJ(27) · 120100 Antiga SEPLAG(28)
120200 SEFAZ LOGISTICA(29) · 123100 IPERJ-Extinto(30) · 123400 RIOPREVIDEN(31) · 123401 RIOPREVI-ALERJ(32)
123402 RIOPREVI-TCE(33) · 123403 RIOPREVI-TJUSTICA(34) · 123404 RIOPREVI-EXECUTIVO(35) · 123410 RIOPREVI-MP(36)
123411 RIOFUNDOPREVI-ALERJ(37) · 123412 RIOFUNDOPREVI-TCE(38) · 123413 RIOFUNDOPREVI-TJ(39)
123414 RIOFUNDOPREVI-EXEC.(40) · 123420 RIOFUNDOPREVI-MP(41) · 123422 RIOPREV-SPSM(42) · 123425 RIOPREV-TXADM(43)
123499 RIOFUNDOPREVI(44) · 124100 CEPERJ(45) · 124200 RJPREV(46) · 130100 SEAPPA(47) · 130200 FUNDEAGRO-RJ(48)
130900 PROJ. RIO RURAL/GEF(49) · 133100 ITERJ(50) · 134100 FIPERJ(51) · 135300 EMATER-RIO(52) · 135400 PESAGRO-RIO(53)
136200 FUNDEAGRO(54) · 137100 CASERJ(55) · 137200 CEASA(56) · 140100 SECC(57) · 144100 FENORTE(58) · 146400 FEFOSP(59)
150100 SECEC(60) · 154100 FUNARJ(61) · 154300 FTMRJ(62) · 154400 FMIS(63) · 156100 FEC-RJ(64) · 160100 SEDEC(65)
166100 FUNESBOM(66) · 170100 SEEL(67) · 173100 SUDERJ(68) · 176100 FUNJOVEM(69) · 180100 SEEDUC(70) · 180300 CEE(71)
190100 SEHAB(72) · 196100 FUNTERJ(73) · 196200 FEHIS(74) · 197100 CEHAB(75) · 200100 SEFAZ(76) · 203100 LOTERJ(77)
206100 FAF(78) · 207100 CFSEC(79) · 210100 SEPLAG(80) · 210110 SUBGERAL(81) · 210600 SSMGSI(82) · 210700 DEGASE(83)
210900 SUBGAP(84) · 213200 RIOMETROPOLE(85) · 213600 PROCON(86) · 215300 SERVE(87) · 216100 FUNDEP(88)
216400 FUSPRJ(89) · 216500 FDRM(90) · 217100 METRO(91) · 217200 CTCRJ(92) · 217300 FLUMITRENS(93) · 220100 SEDEICS(94)
220200 FUNDES(95) · 223200 JUCERJA(96) · 226100 FREMF(97) · 226200 FEMPO(98) · 226300 FSERJ(99) · 227100 CODIN(100)
230100 SEDHSP-GS-Extinta(101) · 240100 SEA(102) · 240200 SEA-PSAM(103) · 240400 FECAM(104) · 243100 IEEA(105)
243200 INEA(106) · 244100 FEEMA(107) · 244200 IEF(108) · 244300 SERLA(109) · 246300 FUNDRHI(110) · 250100 SEAP(111)
254100 FSCABRINI(112) · 256100 FUESP(113) · 260100 SESEG(114) · 260200 SESP(115) · 260400 SEPOL(116) · 261100 SEPM(117)
263100 DETRAN-RJ(118) · 263200 ISP(119) · 266100 ACADEPOL(120) · 266200 SECSP-FUNESPOL(121) · 266400 FUNESSP(122)
266500 FUNNESPOLMILI(123) · 266600 FISED(124) · 280100 SEJDC-Extinta(125) · 290100 SES(126) · 293100 IASERJ(127)
294200 FS(128) · 296100 FES(129) · 297100 IVB(130) · 300100 SETRAB(131) · 300200 FUNRIO(132) · 306100 FEFEPS(133)
306200 FTRJ(134) · 310100 SETRAM(135) · 313300 DETRO-RJ(136) · 316100 FUND.EST.TRANSPORTE(137) · 317100 CODERTE(138)
317200 CENTRAL(139) · 317300 RIOTRILHOS(140) · 320100 SEASDH(141) · 320200 SUBSEC.JUST/DIR.HUM(142) · 324200 F.L.XIII(143)
326100 FEAS(144) · 326400 FUPDE(145) · 350100 SEINPE-Extinta(146) · 353100 DRM(147) · 370100 EGES-SEPLAG(148)
370200 EGE-SEFAZ(149) · 370300 EGE-PREC.JUDICIAIS(150) · 370301 EGE-RPV(151) · 370400 EGE-EMPRESAS EXTINTAS(152)
370500 EGE-DIVIDA PUBLICA(153) · 390100 SUBCOM(154) · 390200 SUBCOM-DESCENTRAL.(155) · 400100 SECTI(156)
400200 GIEDUC(157) · 403200 PRODERJ(158) · 404100 FAPERJ(159) · 404300 UERJ(160) · 404310 A.C(161) · 404320 CEPUERJ(162)
404330 NUSEG(163) · 404340 HUPE(164) · 404350 UERJ-ZO(165) · 404400 FAETEC(166) · 404500 UENF(167) · 404600 CECIERJ(168)
404700 UEZO(169) · 406100 FATEC(170) · 406200 FUNCIERJ(171) · 410100 SEDEB-Extinta(172) · 420100 SEIJ-Extinta(173)
424100 FIA(174) · 426100 FUNDO FIA(175) · 430100 SETUR(176) · 437100 TURISRIO(177) · 444100 FUNDACAO CIDE(178)
450100 SEDRAP(179) · 460100 SEC. ENV. QUAL.VIDA(180) · 470100 SEPROCON(181) · 476100 FEPROCON(182) · 480100 SEPREVDEPQ(183)
486100 FESPREN(184) · 490100 SEDSODH(185) · 496420 FUNDEPI(186) · 500100 CGE(187) · 506100 FACI(188) · 530100 SEIOP(189)
540100 SERGB(190) · 550100 SEVAPD(191) · 570100 SEGOV(192) · 580100 SETD(193) · 590100 SEM(194) · 596100 FEDM(195)
600100 SEIJES(196) · 610100 SEGG(197) · 620200 SEDCON(198) · 630100 SEACJ(199) · 636100 FUNJOVEM(200) · 640100 SEENEMAR(201)
650100 SEHIS(202) · 660100 SECID(203) · 999900 TESOURO ESTADUAL(204) · 999901 SUTES(205) · 999902 SUCOMF(206)
```

---

## 5. Filtro genérico (padrão ADF — quase todas as telas) — **destrava o limite de 1000**

Substituir `PREFIX` pelo prefixo da tela e `TABELA` pelo ID da tabela.

**Estrutura:**
- `PREFIX:TABELA:sdtFilter::head` (cabeçalho "Filtro") · `::disAcr` (expandir/colapsar) · `::btn` · `::body` · `sdtFilter` (container)
- `PREFIX:TABELA:btnClearFilter` (LIMPAR / borrachinha) · `::icon`

**Campos por linha de filtro** (N = índice 0,1,2…):
- `PREFIX:TABELA:table_rtfFilter:N:cbx_col_sel_rtfFilter::content` — SELECT "Propriedade" (campo a filtrar)
- `PREFIX:TABELA:table_rtfFilter:N:chk_neg_rtfFilter::content` — checkbox "Negar"
- `PREFIX:TABELA:table_rtfFilter:N:cbx_op_sel_rtfFilter::content` — SELECT "Operador"
- `PREFIX:TABELA:table_rtfFilter:N:in_value_rtfFilter::content` — INPUT "Valor" (**gerado via PPR** — só
  aparece após selecionar Operador)

**Operadores (value):** `""=Selecione · 0=igual · 1=maior que · 2=maior ou igual · 3=menor que ·
4=menor ou igual · 5=diferente · 6=é nulo · 7=contém · 8=começa com · 9=termina com · 10=pertence (sep. por ;)`

**Sequência correta:** (1) selecionar Propriedade; (2) selecionar Operador; (3) ADF faz PPR e cria o campo
Valor; (4) digitar o Valor; (5) **sem Enter** — a tabela atualiza ao vivo (PPR no blur/change); (6) nova
linha `:1:` aparece sozinha para o próximo filtro (AND implícito entre linhas).

**Limpar:** clicar "Limpar" (visual) ou `document.getElementById('PREFIX:TABELA:btnClearFilter').click()`.

**Limite de registros (o gargalo §8b):**
- Padrão **1000 registros**; aviso em `PREFIX:TABELA:txt_maxResults` ("Limite de 1000 registros.").
- ✅ **Checkbox para remover o limite:** `PREFIX:TABELA:chkRemoveLimit::content` (checkbox). Marcado →
  carrega **todos** os registros (presente p.ex. em Acompanhamento de PD, Código de Barras, Guia de Devolução).
- **Estratégia >1000 sem o checkbox:** (a) filtrar por UG Emitente; (b) por período (Data Emissão ≥ e ≤);
  (c) por Status; (d) por Número "começa com" prefixo; (e) "pertence (;)" para múltiplos valores;
  combinar linhas (AND); iterar em blocos (ex.: 2026OB00001→01000, 01001→02000…).

---

## 6. Toolbar padrão

`PREFIX:TABELA:tbrBotoes` (container) · `btnInsert` · `btnEdit` · `btnView` (Visualizar = double-click) ·
`btnCopiar` · `btnExcluir` · `btnImprimir`.

**Abrir detalhe:** (1) clicar UMA vez na linha (fica azul); (2) clicar `btnView`. O double-click via JS
(`dispatchEvent dblclick`) **não funciona** — precisa ser interação visual.

**Popups padrão:** `popConfirmacaoExclusao · popExclusao · popActivate · popDeactivate · popRestauracao`.

---

## 7. Telas do módulo Execução Financeira

### 7.1 Acompanhamento de Execução de PD — `acompanhamentoExecucaoPDCad.jsp` · prefixo `tpl:` · tabela `tpl:tblAcompanhamento`
Colunas (`tabViewerDec_col_0..8`): Id · Responsável · UG · Descrição · Agendamento · Início · Término · Status · Valor.
Botões: `btnView` · `btnPrint` · `idExecutarNovamente` (Disponibilizar p/ Nova Execução) · `grpButtons`.
Limite: `tpl:tblAcompanhamento:chkRemoveLimit::content` (remove limite). Filtro colapsado.

### 7.2 Bloqueio Judicial — `bloqueioJudicialCad.jsp` · prefixo `pagTemplate:` · tabela `pagTemplate:tblEntidadeDec`
Colunas: Código · Código Credor · Nome Credor · Valor Bloqueado · Data Bloqueio · Data Desbloqueio · Autos do Bloqueio · Status.
Status: Bloqueado, Desbloqueado. Botões (padrão ADF). ⚠️ Usa prefixos `af_` nos ícones — comportamento visual pode diferir.

### 7.3 Código de Barras — `codigoBarrasCad.jsp` (estimado) · prefixo `pt1:` · tabela `pt1:tblEntidadeDec`
Colunas (`col_0..11`): Tipo · Unidade Gestora · Cód. Beneficiário · Beneficiário · Código de Barras · Valor ·
Desconto/Abatimento · Juros/Multa · Ordem bancária · Programação de Desembolso · Observação · Status.
Botões: `btnInsert · btnView · btnImprimir` + link "Importar Código de Barras". Tipo: "Fatura". Status: Pago, Desativado.
Limite: `pt1:tblEntidadeDec:chkRemoveLimit::content`.

### 7.4 Fechamento do Dia — `pagamentoCad.jsp` · prefixo `ptPrincipal:` · tabela `ptPrincipal:tblProcessamentostable`
Colunas (`tblProcessamentostable_col_0..5`): Código · Processo · Data de Início · Data de Término · Usuário · Status.
Botões: `tblProcessamentosviewButton` (Visualizar) · `btnProcessar` (Gerar Prévia) · `btnExecutar` · `btnExcluir` ·
`btnAtualizar` · `tblProcessamentostoolbar`. Popup: `popExclusao`.

### 7.5 Guia de Devolução — `guiaDevolucaoCad.jsp` · prefixo `pt1:` · tabela `pt1:tblGuiaDevolucao`
Colunas (`tabViewerDec_col_0..7`): Número · UG Emitente · Data Emissão · Tipo de Baixa · Status · Cód. UG Liquidante · OB · Valor.
Botões: `btnInsert · btnEdit · btnView · btnCopiar · btnExcluir · btnImprimir`.
Accordion: `pnlAccordionDec_afrCl0` (Filtro) · `pnlAccordionDec_afrCl1` (Conteúdo). Limite: `pt1:tblGuiaDevolucao:chkRemoveLimit::content`.

### 7.6 Guia de Recolhimento — `guiaRecolhimentoCad.jsp` · prefixo `pt1:` · tabela `pt1:tblGuiaRecolhimento`
Colunas (`tabViewerDec_col_0..11`): Número · UG Emitente · UG Orçamentária · UG 2 · Nome UG 2 · Data Emissão · Tipo ·
Status · Estorno · Doc. Alterado · Automática · Valor.
Botões: `btnInsert · btnEdit · btnView · ...` **[mensagem-fonte truncada aqui — completar 7.6+ depois]**

---

## Como o JFN usa isto (operacionalização)
- **`siafe_ob_orcamentaria.py`** (tela 7 / OB Orçamentária `ordemBancariaOrcamentariaCad.jsp`): aplicar a
  sequência da §5 e, sobretudo, marcar `chkRemoveLimit` **ou** iterar por UG (mapa §4) + período p/ romper o
  limite de 1000 (resolve o §8b de `SIAFE-ARQUITETURA.md`).
- **`compliance_agent/ugs.py`**: cruzar o mapa de UGs (§4) com o canônico TFE (lembrar: numeração SIAFE-Rio 2
  ≠ numeração TFE; ITERJ = 133100 nas duas, mas índices diferem).
- **Gotchas que evitam derrubar a sessão:** §2 (popup `myModal`) e §6 (interação visual, não `el.click()`/dblclick JS).

## ⚡ Achados AO VIVO (2026-06-06) — varredura validada da VM
- **A VM acessa o SIAFE!** `siafe2.fazenda.rj.gov.br` respondeu **HTTP 200** da VM (GCP) e o login+navegação
  funcionaram (login ~14s, nav ~28s). **Diferente do SEI**, o WAF do SIAFE **não** está dropando o IP da VM.
  Login real OK com `SIAFE_USER` (CPF) + `SIAFE_PASS` do `.env`. Colhidas/ingeridas **951 OBs de 2026**.
- **A tela OB Orçamentária NÃO tem `chkRemoveLimit`** (confirmado): o `_remover_limite()` loga "checkbox
  ausente". Logo a view sem filtro **trava em ~1000** (951 colhidas; há >10 mil OBs/2026 — nº até `2026OB10361`).
- **O seletor `selUg` NÃO filtra esta tabela** (testado: selecionar SEFAZ/FES/ALERJ via `select_option` nativo
  retornou o **mesmo** conjunto misto de UGs). selUg é contexto, não filtro de listagem.
- **Caminho para romper o teto (a implementar) = o filtro genérico `sdtFilter` (§5).** Sondagem confirmou que
  `pt1:tblOBOrcamentaria:sdtFilter` **existe mas está COLAPSADO** (só `::head/::btn/::disAcr/::body`; os selects
  `cbx_col_sel_rtfFilter`/`cbx_op_sel_rtfFilter` ainda não estão no DOM). **Próximo passo:** clicar
  `sdtFilter::disAcr` p/ expandir → a linha `rtfFilter` aparece → `select_option` em Propriedade ("UG Emitente"
  ou "Data Emissão") + Operador + preencher Valor → PPR refiltra ao vivo → iterar por UG/período (<1000 cada).
  Validar ao vivo (cada iteração ~1-2 min). É o desfecho do §8b sem precisar de replay HTTP/Computer Use.
- **Decisão pendente com o Mestre** antes de implementar o sweep por filtro (custo de runtime + sessão única).

--------------------------------------------------------------------------------
8-B. DOM ESPECÍFICO — OB ORÇAMENTÁRIA (ordemBancariaOrcamentariaCad.jsp)
--------------------------------------------------------------------------------
Tabela: pt1:tblOBOrcamentaria:tabViewerDec   | corpo rolável: ...::db  | scroller: ...::scroller
Cabeçalho: ...::ch     | filtro: pt1:tblOBOrcamentaria:sdtFilter (abrir por ::disAcr)
NÃO há chkRemoveLimit nesta tela (trava ~1000). selUg do topo NÃO filtra esta grade.
23 COLUNAS (ordem do header SIAFE -> nome na base ob_orcamentaria_siafe):
  Número->numero_ob | UG Emitente->ug_emitente | UG Pagadora->ug_pagadora |
  Data Emissão->data_emissao | Status->status | Tipo->tipo | Finalidade->finalidade |
  Tipo de OB->tipo_ob | NL->nl | Credor->credor | Nome do Credor->nome_credor |
  UG Liquidante->ug_liquidante | Valor->valor | Competência->competencia |
  Status de Envio->status_envio | GD->gd | Processo->processo (NÚMERO SEI!) | RE->re |
  PD->pd | Tipo de Regularização->tipo_regularizacao | Qtd. Impressões->qtd_impressoes |
  Assinatura Digital->assinatura_digital | Vinculação de Pagamento->vinculacao_pagamento
Filtro (Propriedade cbx_col_sel_rtfFilter::content, 24 opções):
  0=Número 1=UG Emitente 2=UG Pagadora 3=Data Emissão 4=Status 5=Tipo 6=Finalidade
  7=Tipo de OB 8=NL 9=Credor 10=Nome do Credor 11=UG Liquidante 12=Valor 13=Competência
  14=Status de Envio 15=GD 16=Processo 17=RE 18=PD ... (até 23)
RECEITA p/ furar o 1000 (a implementar, mesma do contratos):
  click sdtFilter::disAcr -> Propriedade=1 (UG Emitente) ou 3 (Data Emissão) ->
  Operador=0 (igual) ou faixa (>=/<=) -> clicar Valor, Ctrl+A/Del -> keyboard.type(valor, delay=80)
  -> Enter. Iterar por UG e, nas grandes (>1000), por faixa de Data Emissão.

--------------------------------------------------------------------------------
8-C. DOM ESPECÍFICO — CONTRATOS (o filtro que JÁ FUNCIONA, validado MGS=41)
--------------------------------------------------------------------------------
Tabela: pt1:tblContrato   | filtro abre por: pt1:tblContrato:sdtFilter::disAcr
Propriedade (cbx_col_sel_rtfFilter::content): 7 = Cod. Contratado (CNPJ)
Operador (cbx_op_sel_rtfFilter::content): 0 = igual
Valor (in_value_rtfFilter::content): DIGITAR com keyboard.type(cnpj_digits, delay=80) + Enter
Código (siafe_contratos.py):
  click_vis("pt1:tblContrato:sdtFilter::disAcr"); sleep 2.5
  locator(Propriedade).select_option("7"); sleep 2.5
  locator(Operador).select_option("0"); sleep 2.5
  v.click(); v.press("Control+a"); v.press("Delete")
  keyboard.type(cnpj_digits, delay=80); keyboard.press("Enter"); sleep 6

================================================================================
6. SESSÃO 2026-06-07 — RECEITA DO FILTRO DA OB VALIDADA (furar o 1000)
================================================================================
VALIDADO AO VIVO (VM, sessão salva, sem MFA):
- AdfPage.PAGE.isSynchronizedWithServer() RETORNA TRUE na view real da OB Orçamentária
  (build 4.167.12). => a solução canônica (trocar sleep fixo por esse wait) FUNCIONA aqui.
  Helper pronto: compliance_agent/siafe_adf.py (classe AdfSync: boot/wait/click_real/
  select_option/type_filter, com fallback gracioso). setAnimationEnabled disponível.
- Painel de filtro da OB abre pelo disclosure 'pt1:tblOBOrcamentaria:sdtFilter::disAcr'.
- IDs REAIS dos selects (infixo table_rtfFilter:0: — NÃO é 'sdtFilter:cbx_col_sel'):
    Propriedade: pt1:tblOBOrcamentaria:table_rtfFilter:0:cbx_col_sel_rtfFilter::content
    Operador:    pt1:tblOBOrcamentaria:table_rtfFilter:0:cbx_op_sel_rtfFilter::content
    Valor:       pt1:tblOBOrcamentaria:table_rtfFilter:0:in_value_rtfFilter::content
- PROPRIEDADES (value->label): 0 Número, 1 UG Emitente, 2 UG Pagadora, 3 Data Emissão,
    4 Status, 5 Tipo, 6 Finalidade, 7 Tipo de OB, 8 NL, 9 Credor, 10 Nome do Credor,
    11 UG Liquidante, 12 Valor, 13 Competência, 14 Status de Envio, 15 GD, 16 Processo,
    17 RE, 18 PD, 19 Tipo de Regularização, 20 Qtd. Impressões, 21 Assinatura, 22 Vinculação.
- OPERADORES: 0 igual, 1 maior que, 2 maior ou igual a, 3 menor que, 4 menor ou igual a,
    5 diferente de, 6 é nulo, 7 contém, 8 começa com, 9 termina com, 10 pertence (;).
- RECEITA: disclosure (clique REAL) -> Propriedade/Operador (select_option) -> Valor
  (keyboard.type+Enter). Esperar AdfSync.wait() a cada passo.
ESTRATÉGIA p/ furar 1000 (cada consulta <1000):
  A) Número "começa com" (prop 0, op 8): "2024OB0".."2024OB9" (10 blocos; subdividir se >1000).
  B) Data Emissão por faixa (prop 3): op 2 (>=) e op 4 (<=) por quinzena (~50/ano).
  C) "pertence (;)" (op 10): agrupar UGs.
ABORDAGENS DA OUTRA IA (avaliação): VÁLIDAS. isSynchronizedWithServer/keyboard.type = corretas
  (confirmadas). MAS o código que ela mandou estava CEGO: IDs errados (faltava table_rtfFilter:0:)
  e ROW_SELECTOR chutado (tr[_afrrk]). NÃO colar; usar os IDs reais acima.
PENDENTE p/ coleta confiável: o _navegar/_login ainda usam sleep fixo -> "context destroyed"
  INTERMITENTE. PRÓXIMO PASSO: integrar AdfSync no _login/_navegar/_colher do
  siafe_ob_orcamentaria.py e adicionar coletar_filtrado(prefixos) -> sweep 2024/25/26.
ERRO A NÃO REPETIR: rodar o sweep em BACKGROUND (nohup OU run_in_background) TRAVA o chromium
  headless no launch (contexto detached). Só FOREGROUND via terminal funciona. E o `timeout`
  deixa chromium órfão segurando a sessão única -> próxima run trava no login: matar o chromium
  (ps comm=chrome + ms-playwright, por PID; NUNCA pkill -f <padrão> que casa o próprio shell).

--- 6.1 UPDATE (validacao do sweep filtrado, 2026-06-07 noite) ---
FUNCIONA: infra do sweep RESUMIVEL (coletar_filtrado): checkpoint POR PREFIXO
(siafe_filtro_ckpt_<ex>.json), ingere por prefixo, e RELOGA continuando de onde parou
(loop de sessoes) — provado no log (login OK / nav OK a cada sessao; sessao SIAFE ~1h).
Disclosure do filtro abre com _click_real (mouse no centro do bbox; locator.click NAO abre).
Propriedade/Operador (select_option) aplicam OK.
GAP (ultimo seletor): o CAMPO VALOR do filtro da OB. O seletor herdado de contratos
('[id*="table_rtfFilter:0"] input[type=text]:visible' .last) da Locator.click timeout 30s na
tela da OB — o input do valor tem estrutura/id diferente em tblOBOrcamentaria. PROXIMO PASSO:
1 probe do DOM do painel ABERTO na OB (listar inputs visiveis dentro de table_rtfFilter:0 e o
id exato do campo valor) e ajustar _F_VAL_SEL.
LICAO DE PROCESSO: rodar foreground com REDIRECT P/ ARQUIVO (pipe perde saida bufferizada no
timeout). E matar python+chromium leftover por PID antes de cada run (timeout deixa o python
async vivo segurando a sessao unica -> proximo run trava no login).

--- 6.2 UPDATE (probe do campo valor, 2026-06-07 noite) ---
ACHADO NOVO: na tela OB Orcamentaria, abrir o filtro mostra so um checkbox (chk_neg_rtfFilter).
Selecionar Propriedade=0 e Operador=8 VIA select_option NAO renderiza o campo VALOR (continua so
o checkbox). No siafe_contratos o select_option faz o valor aparecer; na OB, nao. => o
select_option NAO dispara o valueChangeEvent/autoSubmit do ADF que renderiza o campo valor nesta
tela. PROXIMO PASSO CIRURGICO (1 probe): apos setar o value do select, disparar evento 'change'
real (pg.evaluate dispatchEvent change/blur) OU mudar o select por teclado (focus+ArrowDown/type),
depois re-listar inputs ate o in_value_rtfFilter aparecer. So entao o type_filter funciona.
ALTERNATIVA mais robusta (recomendada se isso resistir): abandonar o browser e ir de PPR HTTP
replay (har2requests/mitmproxy capturando o POST do filtro uma vez) — vide secao 5/6.
