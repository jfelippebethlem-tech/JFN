# HANDOFF 2026-07-30 — plano mestre de 10 fases, execução

**Comece por aqui.** Ponto de partida: `4c3d9316`. Branch: `feat/painel-v15-holo`.

---

## 1 · O QUE MUDOU, EM NÚMERO

| | antes | depois |
|---|---:|---:|
| repo `~/JFN` | 55 G | **39 G** |
| `data/sei_cache` | 26 G | **14 G** |
| `.venv` | 6,3 G | **2,4 G** |
| cache do `uv` | 8,5 G | **5,1 G** |
| `graphify-out` | 301 M | **129 M** (já reindexado) |
| `screenshots` | 418 M | **181 M** |
| `logs` | 26 M | **5,1 M** |
| disco | 57 % (84 G livre) | **49 % (99 G livre)** |
| painel: 1ª carga | 519 KB | **158 KB** |
| painel: cargas seguintes | 519 KB | **1,3 KB** |
| contexto always-on | ~10.300 tok | **~8.500 tok** (ver §4-B: o `analyze` devolveu ~600) |
| detectores no REGISTRO | 39 | **40** (J9) |
| pipelines com SLO | 21 | **23** |

**Nada foi apagado exceto duplicata byte-idêntica**, cada remoção com sha256 e cópia sobrevivente
nomeada em `data/_fase0_dedup.log`. Reversão do torch em `data/_fase0_reversao/`.

---

## 2 · AS TRÊS COISAS QUE EU SUPUS E O CÓDIGO REFUTOU

Registrado porque a próxima pessoa vai formular as mesmas hipóteses.

1. **"`nebulaViva()` vaza `<video>` a cada navegação."** FALSO. Reusa `querySelector` e memoiza o
   HEAD. Medido: 20 navegações, contagem estável em 2. O `+1` que apareceu depois era o
   `nucleo-holo-rj.mp4` que eu acabara de instalar passando a existir.
2. **"Os dois handlers de `pointermove` se sobrepõem."** FALSO. Cobrem conjuntos disjuntos
   (controles × cards) e o comentário no código já dizia. O custo real era `getBoundingClientRect()`
   por evento.
3. **"`markup_linear` ganha do CV quando um item foge da regra."** FALSO. Outlier de +20 % a +60 %
   cega os dois. A vantagem real é **faixa de preço larga com linhas baratas arredondadas** — está
   nomeado no teste. Não refazer pelo caminho do outlier.

---

## 3 · O QUE FOI ACHADO SEM SER PROCURADO

- 🔑 **PAT fino do GitHub embutido no `remoteUrl`** de `.gitnexus/meta.json.bak-secret-*`, com
  permissão **664** numa máquina com outra conta (`opc`). Contido (o `meta.json` vivo, o
  `.git/config` e todo arquivo rastreado estão limpos), permissão corrigida.
  **PENDENTE DO DONO: rotacionar esse PAT.**
- 📸 **`screenshots/02_credentials_filled_*.png`** são prints do login do SIAFE **com as credenciais
  preenchidas**. Eram 664, agora 600. O pipeline de screenshot de debug fotografa o formulário já
  preenchido — vale um guard.
- 🧱 **`TETO_SEM_SUPERFICIE` estava SOLTO em 23 com a dívida real em 0** — a dívida foi paga em
  `cb4dc3bd` e ninguém notou, porque essa catraca não tinha teste de APERTO. Agora tem.
- 🧟 **`podar_cache` nunca teve um caller**, e o docstring dela prometia podar `.json` que o código
  não tocava. 23 GB acumularam por omissão de política, não por decisão.
- 🧟 **`conluio_propostas` nunca teve um caller** — sexto caso de "construído, testado, nunca rodado".
- ⚠️ **`comprimir_caches` validava o comprimido por `tamanho > 0` e apagava o original.** `.gz`
  truncado tem tamanho > 0.
- 📄 **`instrumento_mandato`** — a capacidade que gera o requerimento ALERJ / representação TCE em
  `.docx`, a peça que o gabinete assina — não tinha porta de entrada no painel. A catraca nova achou
  no primeiro uso.

---

## 4 · A "BOLA DO NADA NO MEIO" — os três culpados

1. **`#phand`**: esfera de 190 px criada no centro EXATO, transição de 620 ms, `setTimeout(…,420)`
   arrancando antes, e o `transform` aplicado só no rAF seguinte — que **a 1 FPS demora meio
   segundo**. Ela ficava parada e opaca no meio da tela.
2. **Máscara radial em `51% 46%` contra o núcleo do shader em `41%`** — demonstrável por duas fontes
   independentes no arquivo (`p-vec2(0,0.09)` com `gl_FragCoord.y` para cima; `cy=H*0.41` comentado
   "nasce no núcleo do reator"). Calibrada na era da FOTO parada, nunca reajustada quando o vídeo
   entrou no v30.
3. **`#nucleo::before,.ck-core::before`** — candidato DESCARTADO por ser inerte (`id="nucleo"` tem
   zero ocorrências). Anotado no CSS, não removido.

**E o "piscando" tem um número:** o painel roda a **1-2 FPS com a tela parada**, com 57 animações
`infinite`, 15 delas em `filter`/`box-shadow`/`background-position` (repintura por quadro). A 1 FPS
uma respiração de 2,9 s não respira — pisca. Solução: **modo sóbrio MEDIDO**, com selo visível.

---

## 4-B · RESULTADOS DAS ÚLTIMAS FASES

**Compressão do `sei_cache` (Fase 3), fechada:** 5.887 blobs comprimidos, **682 poupados pela
whitelist de estado vivo**, 150 dentro do TTL de 24 h, **ZERO abortados** — nenhuma falha de
integridade em 5.887 verificações de sha256. 26 G → **14 G**. `siafe_state.json`,
`sei_sweep_progress.json` e o checkpoint de OB seguem intactos e legíveis. Leitura transparente
conferida em 6 blobs sorteados (6/6) e no consumidor real (`relacionados._arquivo_cache` acha o
`.zst` sozinho).

**Manifesto de fotos (Fase 3.2):** 5.525 varridas, **1.221 com pHash da REGIÃO** (22 %; as outras
4.304 a triagem classificou como não-fotografia), 1.643 hashes distintos.
→ **Zero grupos com a mesma foto em processos DIFERENTES.** Nenhuma reciclagem neste acervo. É a
primeira vez que esse zero vale alguma coisa: o "0 recicladas em 2.641 fotos" anterior hasheava o
arquivo inteiro e portanto comparava o **modelo da página**. As 65 repetições achadas são DENTRO do
mesmo processo — anexo duplicado, e a distinção está no código.

**Reindex (Fase 9.1):**

| índice | resultado |
|---|---|
| GitNexus | 1.815 arquivos · 40.149 nós · 60.145 arestas · 945 comunidades, no HEAD |
| Graphify JFN | 25.508 nós · 48.174 arestas · 1.747 comunidades |
| Graphify vault | 3.202 nós · 2.280 arestas · 1.173 comunidades |

⚠️ **O vault ficou pela metade, e é preciso saber disso.** O `graphify update` do CLI é **code-only**:
reconstruiu o grafo mas **não** a camada semântica (rationale/document) das 409 notas — o próprio CLI
diz que isso exige `/graphify --update` pelo assistente, com LLM. Segue pendente.

⚠️ **O `gitnexus analyze` reabriu o ralo de contexto** que eu tinha fechado: regravou o bloco de
~2,5 KB em `AGENTS.md` **e** acrescentou ao `CLAUDE.md`, dobrando o custo. Corrigido, e agora com
trava automática (`tools/gitnexus_enxugar.sh` no pre-commit) — enxugar à mão não segura.

## 4-C · A VERIFICAÇÃO EM 4 LOTES — e as 8 regressões que só ela pegou

**Resultado final: 4.999 passed, 5 skipped, 0 failed, DELTA ZERO** contra `BASE-FALHAS-VM2.txt`.

| lote | resultado |
|---|---|
| 1/4 | 1.198 passed |
| 2/4 | 1.083 passed |
| 3/4 | 1.606 passed, 1 skipped |
| 4/4 | 1.112 passed, 4 skipped |

⚠️ **Mas a primeira rodada teve 8 falhas, e as 8 eram minhas.** Eu havia rodado só o lote 1/4 e
concluído que a Fase 6 estava fechada. Não estava.

| teste | causa |
|---|---|
| `test_painel_vida_viva` (×5) | **terceiro leitor** do painel não reapontado na extração |
| `test_jfn2_skilltree` | `instrumento_mandato` ganhou menu: 28 → 29 itens |
| `test_redacao_conforme` | o vício `propostas_gemeas` precisava ser declarado como CONDUTA |
| `test_catraca_excepts` | +1 captura genérica no sweep novo (estreitada) |

E caçando o terceiro leitor apareceram **mais três**, que ninguém testa e que falhavam em SILÊNCIO:

- **`tools/express_ponte`** lia **ZERO** tokens OKLCH (agora 41). Zero token ali não estoura —
  produz uma paleta vazia, calada.
- **`tools/auditar_contraste`** calculava o `sha1` só da casca de 3 KB. O comentário do próprio
  bloco diz que ele existe porque *"laudo que não diz o que mediu é laudo que pode mentir com
  número certo"* — depois da extração, ele fazia exatamente isso.
- **`tests/test_painel_assinaturas`** procurava `const ASSINATURA={` no HTML para decidir o skip.
  Ficaria pulado **para sempre**, inclusive no dia em que o v14 voltasse — que é precisamente quando
  ele tem de re-armar.

**Lição:** ao extrair CSS/JS de um monolito, `grep -rl 'jfn-painel.html'` e conferir CADA leitor.
Eram sete; achei três na hora, um caiu no teste, e três estavam quebrados sem barulho.

⚠️ **E um erro operacional que custou 4h14 de VM parada.** Montei uma cadeia "espera o lote 2 → roda
o 3 → roda o 4" começando com `until ! pgrep -f 'python -m pytest'`. O `pgrep -f` casa a linha de
comando do PRÓPRIO bash que roda o waiter — que contém essa string. Ela esperou por si mesma, os
lotes 3 e 4 nunca começaram, e eu reportei "em curso" o tempo todo porque o `pgrep` de fato achava
um "pytest": ele mesmo.
**A assinatura estava à vista:** processo com `etime` de horas e **0,0% de CPU** não está rodando,
está esperando. Mesma família de `pkill -f mata a própria sessão`: `-f` casa a linha inteira,
inclusive a sua. Espera boa ancora em ARTEFATO (`until [ -s /tmp/lote4.log ]`) ou guarda o PID.

## 5 · O QUE FICOU PENDENTE (ordenado por valor)

1. ~~**Alimentar `proposta_item`.**~~ ✅ **FECHADO** — o sweep do acervo levou a tabela de 77 para
   **182 linhas**, cada uma com `sha_evidencia`. Ver §4-C e o commit `616aa45f`.
2. **Rotacionar o PAT do GitHub** (§3). Decisão do dono. **Único item que depende só de você.**
3. ~~**Reconstruir a camada SEMÂNTICA do grafo do vault.**~~ ✅ **FECHADO** — 9 subagentes,
   190 notas que nunca tinham passado pela extração. **3.202 → 3.658 nós, 2.280 → 3.894 arestas
   (+71%)**, comunidades 1.173 → 916, conceitos 393 → 587. Zero ponta solta, zero laço, zero nó sem
   `source_file`. Ver §5-B.
4. **Gerar as 4 peças de arte que faltam** — `docs/superpowers/specs/PEDIDO-KEYART-2026-07-30-ATUALIZADO.md`
   tem o prompt passo a passo, com a geometria medida. **Delegado a outro Claude por pedido do dono.**
5. **Popular `tests/BASE-FALHAS-CI.txt`** na primeira rodada real do CI novo (o arquivo explica como,
   e por que ler linha por linha antes de commitar). Depende de um push que dispare o Actions.
6. **O teste do CPF mascarado: `pytest-randomly` instalado, e ele NÃO reproduz.** Instalado em
   2026-07-30 (era o que faltava). O lote 2/4 — o que contém `tests/test_investigacao_dd.py` — passou
   **1112/1112 em quatro ordens aleatórias distintas** (sementes 1, 42, 1337, 20260730), com a
   randomização **provada** (a ordem coletada muda entre sementes, conferido com `--collect-only`).
   Leitura honesta: o gatilho de ordem **não vive dentro do lote 2**, e o protocolo de lotes o evita
   por construção. Isso **não é o mesmo que consertado** — a falha original foi vista na suíte
   monolítica, que a casa proíbe rodar. A instrumentação das 3 saídas silenciosas de
   `_wire_beneficios_pep` segue no lugar: na próxima falha, a mensagem nomeia a causa.
   ⚠️ O plugin ficou **opt-in** (`-p no:randomly` no `addopts` do `pyproject.toml`): embaralhar por
   padrão tornaria o critério de delta-zero não-determinístico. Para caçar ordem-dependência:
   `pytest -p randomly --randomly-seed=<n> $(python -m tools.ci_lote K 4)`.

---

## 5-B · REINDEX TRÍPLICE (Fase 9.1) — os quatro índices

| índice | antes | depois |
|---|---|---|
| **gitnexus** | 40.149 símbolos, 8 commits atrás | **40.278 nós · 60.319 arestas · 948 clusters · 300 flows**, no HEAD (250 s) |
| **graphify JFN** | 25.056 nós / 46.727 arestas | **25.576 nós · 48.287 arestas · 1.735 comunidades**, 0 ponta solta |
| **graphify vault** | 3.202 nós (semântica parada) | **3.658 nós · 3.894 arestas · 916 comunidades** |
| **obsidian** | não medido | **410 notas · 284 problemas reais** (515 menos 231 do artefato derivado) |

**`graphify-out` do JFN: 157 MB → 79 MB**, abaixo da meta de 100 MB da Fase 9, e **sem apagar nada
não-provado**:
- o snapshot `2026-07-30` guardava o bruto **e** o `.zst`, mas os dois **divergiam** (o `.zst` era a
  geração anterior do mesmo dia). Nenhum era duplicata: comprimi o novo em nome próprio
  (`*.pos-reindex.zst`), com sha256 do descomprimido conferido antes de remover o bruto. 30 MB → 3,1 MB.
- **1.070 entradas do cache AST (52,6 MB) não eram referenciadas por índice nenhum** — parses de
  versões de arquivo que já não existem. Arquivadas inteiras em
  `data/_arquivo_frio/ast-orfaos-2026-07-30.tar.zst` (1,2 MB) **após diff de conjunto de nomes**
  provar que nada ficou de fora do pacote. Aritmética que garante que nenhuma entrada VIVA saiu:
  3.044 no disco = 1.974 referenciadas + 1.070 órfãs.
- Descoberta lateral: o `stat-index.json` carrega **843 entradas com caminhos de OUTRA máquina**
  (`/home/jfelippebethlem/JFN/...`). São elas as "860 referenciadas ausentes" — nunca estiveram aqui,
  e não foram criadas pela poda.

**Duas correções de artefato no vault:**
- eu gerava o `GRAPH_REPORT.md` com `obsidian=True`, o que emitia **1.100+ wikilinks
  `[[_COMMUNITY_…]]` para notas inexistentes** — lixo injetado no grafo de links do vault pelo meu
  próprio relatório. Regerado com `obsidian=False`.
- `graphify-out/` entrou em `userIgnoreFilters` do `.obsidian/app.json`. Já estava fora do git e do
  Syncthing; faltava o Obsidian. Derivado não é nota.

**Um arquivo removido do vault, com prova:** o
`sessao-2026-07-29-hermeneutica-osint-e-acervos.sync-conflict-*.md` era **subconjunto estrito** do
canônico (`comm -23` devolveu **zero linhas exclusivas**). Mesmo assim foi **arquivado**, não apagado:
`data/_arquivo_frio/`, com sha256 do descomprimido conferido.

---

## 6 · O QUE NÃO REFAZER

- **Conversão em massa de fotos para AVIF.** Medido: 48 % (não 80 %), 749 MB → 391 MB. Como não se
  apaga o original, **aumentaria** o disco.
- **`markup_linear` com outlier** (§2.3).
- **`©` como marca de banco de imagens.** Acendeu 8× em 7.318 textos, todas em nota fiscal, certidão
  e rodapé de software.
- **EXIF ausente como indício.** O acervo perde EXIF na conversão para PDF; acenderia em quase tudo.
- **Reindexar antes de limpar.** O `.nexusignore` não excluía `graphify-out/` — corrigido na Fase 0.
- **Waiter com `pgrep -f` do próprio padrão.** Auto-casamento: 4h14 de VM parada (§4-C).
- **Confiar num lote só.** O lote 1/4 passou verde com 8 regressões vivas nos outros três.
- **Suíte monolítica.** Use `tools/ci_lote.py` (fatia determinística round-robin, 4 lotes de 109).

---

## 7 · COMO VERIFICAR

```bash
# catracas (rápido, seguro)
.venv/bin/python -m pytest -q -p no:randomly \
  tests/test_divida_except_pass.py tests/test_catraca_excepts.py \
  tests/test_rotas_sem_orfa.py tests/test_rotas_sem_superficie.py \
  tests/test_capacidade_pronta_tem_superficie.py

# suíte em LOTES + delta contra as falhas conhecidas
for k in 1 2 3 4; do
  .venv/bin/python -m pytest -q -rf -p no:randomly -m "not network and not integration" \
    $(PYTHONPATH=. .venv/bin/python -m tools.ci_lote $k 4) 2>&1 | tee /tmp/lote$k.log
done
PYTHONPATH=. .venv/bin/python -m tools.ci_delta /tmp/lote*.log \
  --base tests/BASE-FALHAS-VM2.txt --base tests/BASE-FALHAS-CI.txt

# painel
PYTHONPATH=. .venv/bin/python -m tools.painel_boot_check --todas     # espera 60/60
PYTHONPATH=. .venv/bin/python -m tools.painel_medir_boot             # fps, órbe, geometria

# ⚠️ NÃO espere por processo com `pgrep -f 'python -m pytest'` dentro de um script que
# contém essa string: ele casa a si mesmo e trava para sempre. Ancore num ARTEFATO.
curl -s -H 'Accept-Encoding: gzip' -o /dev/null -w '%{size_download}\n' http://127.0.0.1:8000/
```
